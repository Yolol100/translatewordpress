(function () {
  'use strict';
  // Workflow is registered as a real React TabPanel tab in build/admin/index.js.
  // This file is intentionally kept as a no-op for backwards-compatible handles.
})();
